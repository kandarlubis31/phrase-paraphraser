<div id="results" class="results hidden" style="display: none !important;">
  
  <div class="system-notice-box">
    <div class="notice-header">
      <span class="notice-icon">ℹ</span> SYSTEM_NOTICE: INFRASTRUCTURE_LIMITATION
    </div>
    <p class="notice-body">
      Aplikasi ini berjalan sepenuhnya di atas <strong>Serverless Free Tier Architecture</strong> (Cloudflare Workers & MyMemory API). Infrastruktur ini memiliki batas kuota harian statis. Jika proses mengalami kendala, mohon tunggu beberapa saat sebelum melakukan eksekusi ulang.
    </p>
  </div>

  <div class="progress-step final-result-box" id="step-3">
    <div class="progress-badge" id="badge-3">
      <span class="flag-code">id</span>
    </div>
    <div class="progress-content">
      <div class="progress-title">OUTPUT_EVALUATION
        <span class="progress-status" id="status-3"></span>
      </div>
      <p class="progress-text" id="progress-text-3"></p>
    </div>
  </div>

  <details class="process-dropdown">
    <summary class="dropdown-trigger">
      <span>[+] VIEW_INTERMEDIATE_LOGS</span>
      <span class="chevron">↓</span>
    </summary>
    
    <div class="dropdown-content">
      <div class="progress-chain">
        <div class="progress-step" id="step-0">
          <div class="progress-badge active" id="badge-0">
            <span class="flag-code">id</span>
          </div>
          <div class="progress-content">
            <div class="progress-title">SOURCE_ORIGINAL</div>
            <p class="progress-text-log" id="progress-text-0"></p>
          </div>
        </div>

        <div class="progress-arrow">|</div>

        <div class="progress-step" id="step-1">
          <div class="progress-badge" id="badge-1">
            <span class="flag-code">nl</span>
          </div>
          <div class="progress-content">
            <div class="progress-title">INTERMEDIATE_STAGE_01 [dutch]
              <span class="progress-status" id="status-1"></span>
            </div>
            <p class="progress-text-log" id="progress-text-1"></p>
          </div>
        </div>

        <div class="progress-arrow">|</div>

        <div class="progress-step" id="step-2">
          <div class="progress-badge" id="badge-2">
            <span class="flag-code">en</span>
          </div>
          <div class="progress-content">
            <div class="progress-title">INTERMEDIATE_STAGE_02 [english]
              <span class="progress-status" id="status-2"></span>
            </div>
            <p class="progress-text-log" id="progress-text-2"></p>
          </div>
        </div>
      </div>
    </div>
  </details>

  <div class="result-actions">
    <button id="copyBtn" class="btn-copy">COPY_OUTPUT</button>
  </div>
</div>

<style>
  .results {
    display: flex;
    flex-direction: column;
    gap: 1rem;
    border-top: 1px solid #262626;
    padding-top: 1.25rem;
    animation: fadeUp 0.25s cubic-bezier(0.16, 1, 0.3, 1);
  }

  @keyframes fadeUp {
    from { opacity: 0; transform: translateY(4px); }
    to { opacity: 1; transform: translateY(0); }
  }

  /* ✅ STYLING BANNER SYSTEM NOTICE */
  .system-notice-box {
    background: #171717;
    border: 1px solid #262626;
    border-left: 3px solid #d97706; /* Amber border accent */
    border-radius: 4px;
    padding: 0.75rem 1rem;
    font-family: 'Inter', sans-serif;
  }
  .notice-header {
    font-family: 'JetBrains Mono', monospace;
    font-size: 0.75rem;
    color: #e5e5e5;
    font-weight: 500;
    margin-bottom: 0.35rem;
    display: flex;
    align-items: center;
    gap: 0.4rem;
  }
  .notice-icon {
    color: #d97706;
    font-weight: bold;
  }
  .notice-body {
    font-size: 0.8rem;
    color: #a3a3a3;
    margin: 0;
    line-height: 1.5;
  }
  .notice-body strong {
    color: #d4d4d4;
    font-weight: 500;
  }

  .process-dropdown {
    background: #171717;
    border: 1px solid #262626;
    border-radius: 4px;
    overflow: hidden;
  }

  .dropdown-trigger {
    padding: 0.6rem 1rem;
    font-family: 'JetBrains Mono', monospace;
    font-size: 0.75rem;
    color: #737373;
    cursor: pointer;
    user-select: none;
    display: flex;
    justify-content: space-between;
    align-items: center;
    list-style: none;
  }

  .dropdown-trigger::-webkit-details-marker { display: none; }

  .process-dropdown[open] .chevron { transform: rotate(180deg); }
  .chevron { color: #737373; transition: transform 0.15s ease; }

  .dropdown-content {
    padding: 1rem;
    border-top: 1px solid #262626;
    background: #0a0a0a;
  }

  .progress-chain {
    display: flex;
    flex-direction: column;
    gap: 0.5rem;
  }

  .progress-step {
    display: flex;
    align-items: flex-start;
    gap: 0.85rem;
    padding: 0.85rem 1rem;
    background: #171717;
    border: 1px solid #262626;
    border-radius: 4px;
  }

  .final-result-box {
    border-color: #262626;
    background: #171717;
  }

  /* Indikator Glow Halus Khas Claude Dev-Tools saat Selesai */
  .final-result-box:has(.progress-badge.done) {
    border-color: rgba(16, 185, 129, 0.4);
    box-shadow: 0 0 12px rgba(16, 185, 129, 0.05);
  }

  .progress-badge {
    flex-shrink: 0;
    width: 32px;
    height: 32px;
    display: flex;
    align-items: center;
    justify-content: center;
    border-radius: 4px;
    background: #0a0a0a;
    border: 1px solid #262626;
    font-family: 'JetBrains Mono', monospace;
    font-size: 0.75rem;
    color: #737373;
    text-transform: uppercase;
  }

  .progress-badge.active { border-color: #404040; color: #e5e5e5; }
  .progress-badge.loading { border-color: #d97706; color: #f59e0b; }
  .progress-badge.done { border-color: rgba(16, 185, 129, 0.4); color: #10b981; }
  .progress-badge.error { border-color: #ef4444; color: #ef4444; }

  .progress-content { flex: 1; min-width: 0; }

  .progress-title {
    font-family: 'JetBrains Mono', monospace;
    font-size: 0.75rem;
    font-weight: 500;
    color: #737373;
    margin-bottom: 0.4rem;
    display: flex;
    align-items: center;
    gap: 0.5rem;
  }

  .progress-status {
    display: inline-block;
    font-size: 0.72rem;
    font-family: 'JetBrains Mono', monospace;
    color: #737373;
    margin-left: auto;
  }

  .progress-text {
    font-family: 'Inter', sans-serif;
    font-size: 0.9rem;
    color: #e5e5e5;
    line-height: 1.6;
    margin: 0;
    font-weight: 400;
    word-break: break-word;
    white-space: pre-wrap;
  }

  .progress-text::selection {
    background: #f59e0b;
    color: #0a0a0a;
  }

  .progress-text-log {
    font-family: 'JetBrains Mono', monospace;
    font-size: 0.8rem;
    color: #a3a3a3;
    line-height: 1.5;
    margin: 0;
  }

  /* ✅ GLOBAL HIGHLIGHT INJECT STYLE: Penanda kata yang berhasil diubah oleh mesin */
  :global(.diff-highlight) {
    background-color: rgba(245, 158, 11, 0.15); /* Kuning amber transparan halus */
    color: #f59e0b;
    padding: 0.05rem 0.2rem;
    border-radius: 3px;
    border-bottom: 1px dashed rgba(245, 158, 11, 0.4);
  }

  :global(.diff-highlight::selection) {
    background: #e5e5e5;
    color: #0a0a0a;
  }

  .progress-arrow {
    font-family: 'JetBrains Mono', monospace;
    color: #404040;
    text-align: center;
    font-size: 0.8rem;
    height: 16px;
    line-height: 16px;
    margin: -0.1rem 0;
  }

  .btn-copy {
    font-size: 0.75rem;
    font-weight: 500;
    color: #e5e5e5;
    background: #171717;
    border: 1px solid #262626;
    border-radius: 4px;
    padding: 0.5rem 1rem;
    cursor: pointer;
    font-family: 'JetBrains Mono', monospace;
    transition: border-color 0.15s ease, color 0.15s ease;
  }

  .btn-copy:hover {
    border-color: #404040;
    color: #ffffff;
  }

  @media (max-width: 640px) {
    .progress-step { padding: 0.75rem; }
  }
</style>